import React from 'react';
import AuthProvider from "@/components/providers/AuthProvider";

const Page = () => {
  return (
    <AuthProvider>
      <div>

      </div>
    </AuthProvider>
  );
};

export default Page;